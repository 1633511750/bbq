<view class="wrap">
  <view class="item-wrap">
    <view bindtap="jubao_fun" data-con="{{item}}" class="item" wx:for="{{list_arr}}" wx:key="*this">
      {{item}}
      <text class="right"> > </text>
    </view>
  </view>
</view>
  
